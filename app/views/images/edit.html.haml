%h1 Editing image

= turbo_frame_tag "phone-images-#{@image.phone_id}" do

  = render 'form', image: @image, phone_id: @image.phone_id

  %br

  %p
    = link_to 'Show this image', @image
    |
    = link_to 'Close', :back

