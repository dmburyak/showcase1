= form_with(model: seller, url: sellers_path) do |form_seller|
  - if seller.errors.any?
    %div{style: 'color: red'}
      %h2
        = pluralize(seller.errors.count, 'error')
        prohibited this seller from being saved:
      %ul
        - seller.errors.each do |error|
          %li= error.full_message
  %div
    = form_seller.label :name, style: 'display: block'
    = form_seller.text_field :name
  %div
    = form_seller.submit
