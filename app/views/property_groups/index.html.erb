<p style="color: green"><%= notice %></p>

<h1>Property groups</h1>

<div id="property_groups">
  <% @property_groups.each do |property_group| %>
    <%= render property_group %>
    <p>
      <%= link_to 'Show this property group', property_group %>
    </p>
  <% end %>
</div>

<%= link_to 'New property group', new_property_group_path %>
