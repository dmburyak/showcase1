.row.row-cols-1.row-cols-md-1.row-cols-lg-2.row-cols-xl-3
  - @phones.each do |phone|
    .phone-card
      .col
        .card.shadow-sm
          .card-body
            .card-text
              = link_to (render 'phone', phone: phone), phone