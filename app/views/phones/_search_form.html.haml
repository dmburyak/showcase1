= search_form_for(@q, html: { class: 'd-flex', role: 'search' }) do |f|

  = f.search_field :name_or_description_or_property_values_property_data_cont, class: %w[form-control me-2],
  'aria-label': 'Search', placeholder: 'Search', type: 'search'

  = f.submit class: %w[btn btn-outline-primary], type: 'submit'