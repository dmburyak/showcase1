= turbo_frame_tag "phone-images-#{@image.phone_id}" do

  %p{style: 'color: green'}
    = notice

  = render @image

  %div
    = link_to 'Edit this image', edit_image_path(@image)
    |
    = link_to 'Close', :back
    |
    = link_to 'Destroy', phone_path(@image),
    data: { turbo_method: :delete, turbo_confirm: 'DELETE? Are you sure?' }
