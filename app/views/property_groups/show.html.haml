%p{style: 'color: green'}= notice
= render 'property_group', property_group: @property_group
%div
  = link_to 'Edit this property group', edit_property_group_path(@property_group)
  |
  =link_to 'Back to property groups', property_groups_path
  %br
  %br
  = link_to 'Destroy', property_group_path(@property_group),
    data: {turbo_method: :delete, turbo_confirm: 'DELETE? Are you sure?'}
